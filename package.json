{
	"name": "insider",
	"private": true,
	"type": "module",
	"scripts": {
		"dev:site": "node --env-file=.env dev-server.mjs",
		"check:types": "cd worker && tsc --noEmit",
		"build:site": "mkdir -p dist/site && cp index.html map.html dist/site/"
	},
	"devDependencies": {
		"@types/node": "^22.10.10",
		"typescript": "^7.0.2"
	}
}
